|> Root
    attr1=val1

    \ I'm a text line with leading space
    \I'm a text line with a backslash
    \ I'm a text line with leading backslash
    hey

    \I also start with a backslash

    |> Child1
        child1Attr1=child1val1
        child1Attr2=child1val2
        attr1= invalidAttr

        some text

        $$
        \displaylines
        $$

    |> Child2
        = text not an attribute

        |> Grandchild1
            Also an invalid=attr

    |> Child3
        attr1=val1

    some text

    |> Exercise
        In the solution to Exercise >>TrueOrFalse, we
        observed how the difference

        |> TextParent
            |> ul
                style=margin:20px 20px 20px 50px;list-style-type:disc;

                |> li
                    style=margin-bottom:10px;

                    in 
                    |> span
                        style=border:1px solid blue
                        blue
                    , particle 1, the last particle
                    to be (noticeably*) “peeled off” from
                    the circle (*all particles are
                    instantaneously peeled off from the
                    circle to _some_ degree, as one
                    particle's slight deviation affects
                    the next, that affects the next, etc)
